<nav class="navbar navbar-expand-lg navbar-dark navbar-bg-custom">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">XtremSnowTricks</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home') }}">{% trans %}snow_trick.navbar.home{% endtrans %}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link"
                       href="#">{% trans %}snow_trick.navbar.tricks{% endtrans %}</a>
                </li>
                {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link"
                           href="#">{% trans %}snow_trick.navbar.up{% endtrans %}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link login-button"
                           href="{{ path('app_login') }}">{% trans %}snow_trick.navbar.in{% endtrans %}</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link login-button"
                           href="{{ path('app_logout') }}">{% trans %}snow_trick.navbar.out{% endtrans %}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>