{% extends 'base.html.twig' %}

{% block title %}{% trans %}snow_trick.title.home{% endtrans %}{% endblock %}


{% block body %}
    <div class="container arrow-container p-0 mb-5 fs-custom-card" {{ stimulus_controller('pagination-trick-homepage', {
        url: path('home'),
        page: page,
        pageMax: page_max ,
        arrowPageUpOn: arrow_page_up_on,
        trickPerPage: trick_per_page
    }) }}>
        <div class="row mt-4 mb-4" id="tricks-list" data-pagination-trick-homepage-target="result">
            {{ include('home/_home.html.twig') }}
        </div>
        <div id="tricks-loader">
            {{ include('home/_loader.html.twig') }}

        </div>
        {% include 'component/_arrow_page_up.html.twig' %}
    </div>
{% endblock %}
